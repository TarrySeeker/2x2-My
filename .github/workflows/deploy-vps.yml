# DEPRECATED — этот workflow заменён на deploy-docker.yml в Chain 3.
# Старый PM2-деплой больше не используется.
# TODO: удалить этот файл при коммите Chain 3.
name: DEPRECATED — Deploy to Timeweb VPS (PM2, legacy)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated. Use deploy-docker.yml instead."
          exit 1
