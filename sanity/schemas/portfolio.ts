/**
 * Sanity удалён на Этапе 1. Схема переехала в supabase/migrations/
 * (таблица portfolio_items).
 */
const portfolio = {
  name: "portfolio",
  title: "Работа в портфолио",
  type: "document",
  fields: [],
};

export default portfolio;
