/**
 * Тексты cookie-баннера.
 *
 * Используется в компоненте CookieBanner (Этап 4).
 * Хранится здесь как TS-константа — фронтенд импортирует напрямую.
 *
 * Ведёт: content-manager (Этап 6, 2026-04-23).
 */

export const cookieBanner = {
  /** Заголовок баннера (3–5 слов) */
  title: 'Мы используем cookies',

  /**
   * Текст-пояснение (1–2 предложения).
   * Ссылка на политику вставляется компонентом через поле policyUrl/policyText.
   */
  body: 'Сайт использует файлы cookie для аналитики и улучшения работы сервиса.',

  /** Текст ссылки на политику конфиденциальности */
  policyText: 'Подробнее в Политике конфиденциальности',

  /** URL политики конфиденциальности */
  policyUrl: '/privacy',

  /** Кнопка принятия всех cookies */
  acceptLabel: 'Принять',

  /** Кнопка отклонения аналитических cookies (технические останутся) */
  declineLabel: 'Отклонить',

  /**
   * Aria-label для кнопки закрытия баннера (крестик).
   * Закрытие без выбора = «Отклонить».
   */
  closeAriaLabel: 'Закрыть уведомление о cookies',

  /**
   * Ключ localStorage, по которому хранится решение пользователя.
   * Значения: 'accepted' | 'declined'.
   * Если ключа нет — баннер показывается.
   */
  storageKey: 'cookie_consent',
} as const

export type CookieBannerContent = typeof cookieBanner
